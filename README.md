# user-authentication
 so this is a simple user authentication using react in frontend and node js in backend using mysql as database . This design lacks quite a functionalities like password encryption and body parser . but it works quite fine. If anyoen wants to develop this can surely do that and i will merge in the main as well.
